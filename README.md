# stripe-fixtures-skills

Agent Skill for Claude Code / Cursor / etc.

## Installation

### via gh skill
```bash
gh skill install <your-github-id>/stripe-fixtures-skills stripe-fixtures --pin v0.1.0
```

### via npx skills
```bash
npx skills add <your-github-id>/stripe-fixtures-skills --skill stripe-fixtures
```
